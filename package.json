{
  "name": "antguessr",
  "version": "0.0.1",
  "main": "back/index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "export NODE_ENV=development; concurrently -n \"front,back\" -c magenta,blue \"cd front && npm run dev\" \"cd back && npm run dev\"",
    "build": "npm ci && pushd back && npm ci && npm build && popd && pushd front && npm ci && npm run build && popd"
  },
  "author": "laggycomputer",
  "license": "UNLICENSED",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.0",
    "openapi-typescript": "^7.4.4",
    "typescript": "^5.7.2"
  }
}
